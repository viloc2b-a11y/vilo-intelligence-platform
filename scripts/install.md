# Install Notes

VIP currently has no runtime vendor integrations.

## Local Setup

```bash
npm install
npm test
```

## Environment

No secrets are required for this foundation. Do not add provider keys, database URLs, or product credentials to this repository.

## SQL

SQL files under `supabase/sql/` are draft-only and must not be applied automatically.
