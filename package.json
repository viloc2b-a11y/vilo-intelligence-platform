{
  "name": "vilo-intelligence-platform",
  "version": "0.1.0",
  "private": true,
  "description": "VIP, a provider-agnostic reusable intelligence harness for Vilo products.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/vip-packs/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "health": "tsx scripts/health.ts",
    "seed:clinical": "tsx packages/vip-packs/clinical/seed.ts",
    "test": "npm run build && node --test dist/__tests__/*.test.js",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "tsx": "^4.22.3",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
