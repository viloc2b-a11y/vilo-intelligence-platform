# VIP SQL Foundation

VIP does not assume Supabase exists yet. These files are SQL foundation drafts and must not be applied automatically.

Apply manually, in this exact order:

1. `vip-schema.sql`
2. `vip-memory.sql`
3. `vip-hardening.sql`
